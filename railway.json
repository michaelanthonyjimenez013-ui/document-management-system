{
  "build": {
    "commands": [
      "apt-get update && apt-get install -y libpng-dev libonig-dev libxml2-dev && docker-php-ext-install pdo pdo_mysql gd mbstring xml",
      "composer install --no-dev --optimize-autoloader",
      "php artisan config:clear"
    ]
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
